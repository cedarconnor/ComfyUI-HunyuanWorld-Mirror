# Minimal requirements for HunyuanWorld-Mirror ComfyUI Node Pack
#
# This file only includes packages that are NOT typically part of the
# base ComfyUI installation. Use this if you're getting version conflicts.
#
# Usage: pip install -r requirements-minimal.txt

# 3D Processing and Export (core functionality - usually not in ComfyUI)
trimesh>=4.0.0
plyfile>=1.0.0
open3d>=0.18.0

# Utilities (if not already installed)
timm>=0.6.7
omegaconf>=2.3.0

# gsplat dependencies
ninja
jaxtyping
rich
